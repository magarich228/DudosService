
let inputContainers = document.querySelectorAll('.email-input-container');
let addButton = document.querySelector('.email-input-add-button');
let removeButtons = document.querySelectorAll('.email-input-remove-button');

addButton.addEventListener('click', (obj) => {
    
});

inputContainers.forEach((element) => {
    element.addEventListener('mouseover', (e) => {
        e.target
    });

    element.addEventListener('mouseout', (e) => {

    });
});